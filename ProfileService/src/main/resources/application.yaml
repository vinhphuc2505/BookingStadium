server:
  port: 8082
  servlet:
    context-path: /profile



spring:
  application:
    name: profile-service
  data:
    mongodb:
      uri: mongodb://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/
      database: ${DB_NAME}

